en este vídeo voy a hablar de los objetos de composición los objetos de composición nos permiten combinar volúmenes dentro de 3d studio max mediante una operación principalmente llamada operación booleana encontramos más tipos de objetos de composición dentro del menú correspondiente de tres estudio max pero el principal tipo de estos objetos de composición como decía es la operación booleana para continuar por tanto lo que voy a hacer es no ocultar o no eliminar los objetos que crean hasta ahora sino que los voy a introducir dentro de una nueva capa por tanto lo que haré es crear una nueva capa le voy a poner el nombre escenario veo que como tenía seleccionado el fondo ya ha pasado a formar parte de esta capa pero quiero que los textos también formen parte por tanto lo que voy a hacer es seleccionar ambos textos a continuación voy a seleccionar la capa de escenario por lo que por último pulsar el botón que añadirá el texto secundario y el texto principal junto con la capa de fondo a la capa escenario las dos últimas operaciones que voy a hacer antes de combinar van a ser por una parte activar la capa 0 la capa default como capa activa y por último ocultar la capa escenario es decir quiero continuar trabajando con este proyecto pero no quiero que la capa de escenario me moleste o me distraiga un error muy común suele ser el que olvidemos hacer que la capa default sea la capa activa cuidado porque de esta manera la capa activa ahora mismo en ese escenario estaría oculta lo cual quiere decir que si empiezo a dibujar un objeto por ejemplo voy a dibujar una caja dibujaré algo pero realmente no lo veré si me pasa esto es un error muy frecuente simplemente por tener en cuenta que es que la capa activa es la capa de escenario y la capa que debo tener activa para trabajar correctamente es la capa 0 default donde crearé objetos con toda la normalidad por cierto que acabo de crear una caja de la capa escenario así que la voy a borrar y voy a volver a ocultar como decía esta capa escenario para seguir trabajando para seguir creando nuevos objetos en la capa 0 default