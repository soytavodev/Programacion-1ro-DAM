existe una variación el bucle de control del foro específica para el trabajo con matrices por tanto dentro del método principal lo que voy a hacer es crear una matriz voy a llamar a la matriz y mi matriz y le voy a ganar el corchetes a indicar que es una matriz y automáticamente la inicial hizo con valores por tanto le digo que los valores son 1 2 3 4 sin saber todavía el número de valores que va a tener esta matriz de momento he puesto cuando voy a crear voy a inicializar también una variable alemana suma que de momento voy a igualar a cero y tras estas dos matrices y variable con el correspondientemente lo que voy a hacer es crear un bucle foro que va a tener una validación y va a tener un código y en este caso lo que voy a decirle es que lo que hago es crear una nueva variable entera llamada x pongo x 2 puntos mi matriz tras esto el código que contiene la matriz es zuma más igual a x lo que estoy haciendo es que a la suma le sumó el valor de la matriz y por último voy a crear un sistema punto out punto green line donde colocar el valor de la zona si ejecutó este código comprobaré lo siguiente lo que he hecho es sumar los valores dentro de la matriz y si a uno le sumó 2 314 lo que tengo es lo que yo con este foro es y sumar un nuevo 1 los valores de la matriz la ventaja de este bucle form insisto especializado para matrices es que no requiere inicializar un parámetro no requiere una condición de final ni requiere un contador porque lo que hacemos en este caso es pasear uno a uno es decir debe revisar ir uno a uno de los valores de la matriz y trabajarlos dentro del bucle for no necesito decir la condición inicial ya que cuando digo x dos puntos y la matriz lo que hace el programa es automáticamente empezar por el primer valor de la matriz sea cual sea el primer valor de la matriz no necesito decirle condición y final porque cuando acabe con los valores de la matriz automáticamente cuando llegue el último va para el bucle foro y tampoco necesita decirle el incremento ya que va a ir uno a uno de los valores de esta matriz o de la matriz que pongamos por tanto esa manera mucho más sencilla de trabajar un bucle for y especializada para combinar bucles foro será poco aquí 4 5 6 7 y 8 lo que obtengo cuando le doy a play al programa cuando ejecutó el programa es que la suma es igual a 36 es decir poniendo esto dice modificarlo automáticamente el núcleo for detectar dónde está el principio donde está el final y va 11 dentro de los elementos haciendo la operación que necesite hacer con ellos