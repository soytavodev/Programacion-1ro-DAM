el modificador screw es el que conocemos en otros programas como modificador torno o como modificador Revolución vamos a eliminar todos los objetos vamos a una vista frontal por ejemplo vamos a crear una un cubo y entrando en modo edición voy a crear los vértices con control click y extrusión voy a crear los vértices correspondientes al perfil de revolución el ejercicio clásico pasa por dibujar una copa voy a dibujar una copa con la letra e lo que estoy haciendo es destruir un vértice voy a eliminar los vértices que me sobran Y si aplicamos el modificador screw vamos a alinear estos vértices con el punto de pivote que lo vemos aquí en esta parte de la pantalla lo que haremos con el modificador screw es aplicárselo a este objeto y lo que conseguiremos es hacer una Revolución el modificador screw nos permite definir el ángulo que queremos barrer que por defecto es de 360 gr es decir una vuelta entera la cantidad de pasos que queremos dar lo que sería la calidad de la Revolución y la cantidad de pasos que tendremos en el render las iteraciones que vamos a tener y el valor de paralaje en este caso por si queremos hacer una escalera de caracol La barandilla de una escalera de caracol podemos hacer la revolución en X en y o en Z pero básicamente todo si pongo menos segmentos pongo pocos segmentos y le aplico un modificador de subdivisión podemos obtener los vértices de por ejemplo en este caso una copa y la gran ventaja es que si lo aplicamos como modificador en todo caso Siempre vamos a poder editar los vértices y volver para visualizar el cambio de resultado pagamos y grabamos