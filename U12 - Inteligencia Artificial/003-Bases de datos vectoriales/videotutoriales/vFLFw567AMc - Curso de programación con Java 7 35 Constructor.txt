vamos a hablar a continuación de los constructores la utilidad de los constructores sobre todo es cuando la tienen cuando llamamos un objeto sin haberle pasado parámetros vamos a borrar todo este código no voy a usar ahora escáner por tanto lo voy a borrar de momento y lo que voy a hacer es llamar a múltiples métodos voy a llamar voy a hacer multi los métodos múltiples métodos objetos es igual a una nueva instancia de múltiples métodos y sin de momento sin argumentos la cuestión es que si ahora llamo a múltiples métodos objeto punto y uso el digo y ejecutó este código lo que voy a tener en este caso evidentemente es que el nombre de tu amigo es nulo ya que por supuesto no le he pasado ningún parámetro y al no pasar en ningún parámetro lo que hace es no poder ofrecerme ningún valor en este caso los constructores son una solución a este problema los constructores lo que permiten es inicializar el valor de variables o de métodos antes de empezar a trabajar con ellos e incluso antes de pasarles variables por tanto vamos a ir a múltiples métodos punto java y vamos a crear un método para esta clase vamos por tanto a él el archivo múltiples métodos punto java nos metemos dentro de la clase pública múltiples métodos y lo que vamos a hacer ahora justo bajo de esta variable es crear lo siguiente vamos a crear un método con la única condición de que el nombre del método tiene que ser exactamente igual al nombre de la clase los constructores deben cumplir esta condición es que el nombre del método tiene que ser exactamente igual al nombre de la clase i lo que voy a hacer ahora es crear un método público llamado múltiples métodos exactamente igual que el nombre de la clase y voy a poner strings se crearon strings que va a ser el nombre y lo que ahora es que el nombre amigo será igual a name tengo ahora por tanto esto que tengo aquí es el constructor correspondiente a la clase múltiples métodos es un constructor que consiste en un método en sí es una vez más el nombre del método es igual al nombre de la clase esto es muy interesante porque a continuación va a permitir volver a josé vicente punto java y poner por ejemplo new múltiples métodos con un parámetro llamado por ejemplo javier si ahora por ejemplo digo aquí múltiples métodos objetos digo lo que tengo es que el nombre de tu amigo es javier esto es muy interesante ya que este constructor nos permite ahorrar como podemos ver bastante código y sobre todo es interesante por otra cosa y es que nos permite crear múltiples instancias de objetos por ejemplo múltiples objetos o múltiples métodos objetos dos es igual a una nueva instancia de múltiples métodos con el nombre miguel si ahora llamo a múltiples métodos objetos digo porque tengo es en el medio amigo es javier pero si ahora llamo a múltiples métodos objeto dos juntos dijo lo que obtengo es el nombre de tu amigo es javier el número de tu amigo es miguel que tengo aquí pues lo que he hecho es crear una nueva instancia de múltiples métodos con el parámetro javier y lo que estoy haciendo es crear una nueva instancia con un set completo de variables específicas y propias de ese objeto es decir siendo el objeto una plantilla lo que he hecho es crear un calco optimizado con este nombre de variable de esa plantilla ya continuación lo que hago es a partir de esa plantilla en blanco crear un nuevo calco llamado múltiples métodos objetos dos con el parámetro miguel por tanto lo que es hacer nuevo calco de múltiples métodos objeto 2 con en este caso el set de parámetros el que parte a partir de la variable miguel y esto es muy interesante unido al hecho de que creando un constructor lo que podemos hacer es arrancar una variable incluso antes de pasarle un parámetro esta clase