voy a borrar ahora este código ya hemos visto cómo funciona y voy a deciros que a partir de ahora cargar 'aces' dos etiquetas de hecho si queréis voy a duplicar esta de aquí en esta primera no voy a poner un modificador voy a poner un atributo un parámetro llamado src y voy a cargar la librería mira me voy a llama script cargo esa librería copio el nombre y la pego ahí y aquí pongo este es código nuestro archivo va a tener siempre por tanto está estructura en primer lugar va a tener una etiqueta script que se abre y se cierra fijaos que también se cierra donde lo importante es un parámetro en el que pone src jay query 1 punto lo que sea punto mm punto j s que estoy haciendo allí estoy cargando jay query estoy cargando la librería para que todos los métodos empiecen a estar disponibles para que pueda empezar a utilizarlos y después a continuación no antes sino después voy a poner otra etiqueta script donde ahí voy a escribir mi código ahí voy a escribir el código pero es importante que haya descargado antes la librería de query para que cuando lleguemos a esta librería el navegador cuente con las funciones que están introducidas aquí dentro así que nunca inviertas el orden lo primero que vamos a hacer siempre dentro de javascript es crear una función llamada inicio el inicio lo voy a escribir de la siguiente manera escribo dólar paréntesis de paréntesis document y esto es lo que se llama un selector lo que hago es poner dólar paréntesis de apertura para empresas de cierre y dentro de los paréntesis escribo lo que seleccionó en este caso que estoy seleccionando el documento a que equivale la palabra documento a decir selecciono toda la página web selecciono el nivel de jerarquía más grande que se pueda solucionar que se pueda seleccionar a continuación que ya he seleccionado el documento pero en quart y en javascript en general no es ya sólo importante decir a quien quiere seleccionar sino qué quieres hacer con ello por tanto una vez que tienes a quien ha seleccionado escribes un punto y dices qué quieres hacer con ello voy a usar una función llamada 9 y esto básicamente en castellano se lee cuando el documento esté preparado cuando el documento está a punto red y ejecutó una función llamada inicio una vez más y como siempre os digo que todo aquello que esté en inglés no lo puedo cambiar tiene que escribirse exactamente como yo lo escrito pero todo aquello que esté en castellano y la palabra de inicio está en castellano se puede cambiar por la palabra que queráis por la palabra que queráis siempre siguiendo unas cuantas reglas no ponen espacios no ponen acentos no ponen en es y cosas así en mayúsculas se admiten aunque a mí no me gusta especialmente usarlas ahora he dicho que cuando el documento esté preparado cargo la función de inicio pero dónde está la función de inicio que es la función de inicio pues la función de inicio es una función que escribo a continuación function inicio y pongo function inicio paréntesis que se abren paréntesis que se cierran y explicaré por qué y llave que se abre y llave que se cierra entre la llave que se abre y la llave que se cierra voy a escribir todo el código que va a tener la función de inicio así que básicamente lo que hecho en la pantalla es un cargador cuando el documento esté preparado carga me la función de inicio la función inicio va a tener el siguiente código que hace el punto 9 lo que hace es que el javascript no carga hasta que no he llegado a leer la última línea de html y así me aseguro que lleva escrito nunca va a dar errores porque esté situado en la cabeza en lugar de estar situado en el final de la página html