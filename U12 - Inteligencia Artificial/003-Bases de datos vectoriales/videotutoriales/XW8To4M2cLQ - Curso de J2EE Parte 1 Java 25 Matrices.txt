las matrices son parecidas a las variables salvo que en lugar de permitirnos asociar un único valor a una variable nos permite añadir un montón de valores os permite añadir un set de valores las matrices tienen la siguiente estructura voy a crear una variable en una variable entera le llamaría variable no la voy a escribir variable para no confundir voy a verla mi variable puede tener perfectamente cualquier memoria simple tiene puede tener perfectamente cualquier nombre la única diferencia es que en lugar de inicializar la como en mi variable o inicializar la como en mi variable y dos corchetes de los corchetes indica que esto no es una variable sino que es una matriz aunque existe o que una matriz se trabaja muy parecida como ahora veremos a una variable salvo con la única diferencia de que contiene un índice ahora veremos qué es esto de los índices esta variable va a tener 10 índices por tanto voy a poner mi int lo que te puedo inicializar es una matriz que va a tener 10 índices las matrices tienen índices y valores asociados a esos índices hasta ahora éramos capaces de decir esto mi variable era igual a 3 por ejemplo por tanto lo que hacíamos es que cada vez que llegábamos a mi variable nos daba el resultado 3 pero ahora lo que vamos a hacer es tener mi variable con índices porque por tanto la variable con índices 0 es igual a 3 la variable mi variable con índice 1 es igual por ejemplo 5 mi bar y hable voy a copiar estas dos líneas y voy a poner 2 3 4 5 y de introducir valores 7 2 1 lo que hago al ejecutar el programa de momento es nada porque lo único que he hecho o lo único que devuelvo en este caso es nada ya que lo que estoy haciendo es almacenar en la memoria el valor de las matrices de los índices de la matriz así que ejecutó el programa y me dice que el programa se ha ejecutado correctamente aunque no devuelve nada en la pantalla vamos a hacer que devuelva algo en la pantalla y para eso y ahora vamos a empezar a ver la utilidad y el funcionamiento de las matrices voy a hacer lo siguiente system punto out punto print line y aquí dentro lo que voy a hacer sin comillas es devolver el valor de mi variable cuando tiene el índice 1 si ahora ejecutó el programa comprobaré como si pido el aquel índice de la variable que tiene el índice 1 el valor de vuelto es 5 y compruebo que aquí tengo 5 qué pasa si pido el índice número 2 lo que tengo es que se me devuelve evidentemente el número 7 por tanto las matrices convierten las variables en digamos multi variables una misma variable puede tener múltiples contenidos múltiples asociaciones eso sí siempre teniendo en cuenta y siempre respetando esta pareja este número es el índice y este número de aquí en este caso número puede ser cadenas pero este número de aquí en este caso es el valor asociado a este índice por tanto en este caso 3 está asociado al índice 0 de la matriz mi variable 5 está asociado al índice 1 de la matriz muy variable y así pudo hacer más matrices y puedo decir ahora que mi matriz y si ahora le digo pues por ejemplo cambio todo esto y aquí pongo mi matriz vamos a cambiar todo esto y por ejemplo el índice de 2 a mi matriz en el número 3 mientras que el índice 2 de mi variable ti en el número 7 si pido el índice 2 de muy variable y ejecutó el programa obtendré el número 7 pero sí pido el índice de los de mi matriz y ejecutó el programa obtendré el número 3 comprobamos por tanto como las matrices consisten en elementos asociados a los índices de cada una de las matrices