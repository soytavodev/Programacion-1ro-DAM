la herencia es una característica de java que nos permite pasar fácilmente el código de unas clases a otras en este caso estoy trabajando con josé vicente punto java y he creado un archivo llamado superclase punto java y subclase punto java lo que hago dentro de superclase es crear un método público void hola sin parámetros que contiene un código tan sencillo como system punto out punto print line y entre paréntesis y comillas digo yo digo hola y ahora quiero que su clase r de lo el código los métodos y las propiedades de superclase lo que tengo que hacer es ir a su clase punto java y especificar el siguiente código voy a poner su clase public clases son clase extensa extienden extiende es la palabra mágica y reservada para llamar a la herencia superclase lo que hago de esta manera es que automáticamente subclase aunque no se vea aquí abajo hereda las propiedades y los métodos de superclase automáticamente por poner subclase extend superclase subclase ha heredado el método public void hola con este código que tengo aquí vamos a demostrarlo dentro de josé vicente punto java otra cosa que voy a decir por eso nombrado así a las clases es que cuando tengo algo así cuando tengo una clase heredada y una clase que hereda la clase a la que llamó se llama superclase no hace falta llamar al archivo así lo he llamado para que coincida y la clase que extiende es la subclase por tanto en este caso esta es la subclase esta es la superclase una vez más insisto en que no tienen por qué llamarse subclase y superclase lo he hecho por la claridad ejercicio y ahora lo que voy a hacer es dentro de josé vicente punto java voy a crear una nueva instancia de subclase por tanto hago subclase objeto es igual a una nueva instancia d subclase sin parámetros yo lo que hago es llamar a su clase de objeto y compruebo como al poner punto me da como disponible el método hola el método o la que he creado en subclase como es posible esto sí subclase no tiene ese método hola porque el método hola está en la superclase y cuando le digo que el método en la clase subclase extiende a la clase superclase automáticamente la clase subclase pasa a tener el método hola si ahora ejecuto esto será le doy a play comprobar como el resultado del ejercicio es yo digo hola es decir he comprobado como gracias a la herencia subclase puede disponer del método hola y por tanto al crear un nuevo objeto desde jose vicente josé vicente llamadas un clase y su clase llama a superclase de esta manera voy pasando métodos y propiedades de una clase a otra de una manera como vemos bastante sencilla simplemente lo único que tenemos tener en cuenta es la palabra mágica externa qué me sirve para decir que esta clase extiende a esta otra es decir la clase subclase hereda automáticamente propiedades y métodos de la clase llamada superclase