vamos a ver en este vídeo como una matriz puede no sólo contener variables sino incluso también contener objetos por tanto voy a hacer un nuevo proyecto llamado un contenedor y dentro de este proyecto voy a crear una nueva clase de java llamada lista de objetos lista objetos finalizó y ahora dentro de esta clase voy a crear una lista de objetos voy a crear una matriz de tipo privado que se va a llamar solución que se va a llamar un objeto a hacer una matriz y lo vamos a llamar la lista de posiciones de objetos será igual a una nueva matriz mi objeto con cinco elementos en la matriz tenemos por tanto es un objeto de cinco elementos cara que tenemos por aquí y de momento no puede este siglo lo que vamos a hacer es crear un objeto vamos a crear una nueva clase llamada mi objeto para que a partir de aquí ya pueda encontrar mi objeto correctamente aquí lo que estamos haciendo es crear una nueva instancia de una clase llamada virgen está bien vamos ahora a decir que construyamos un contador de contador que es igual a 0 voy a decir que ese tipo entero y así creamos un mercado público y por voice en el que añadimos en este método se llama a la vida y en amigos ni objetos con el número d dentro de este método una estructura de control y sangrías correctamente para poder ver bien el código y decimos que si el contador es menor lista de objetos junto leer qué es esto lo que va a hacer es contar automáticamente cuál es el límite cuál es la longitud de esa lista de objetos lo que haremos es decirlo diremos que y lista de objetos con el índice contador será igual a d ahora tenemos hasta el punto a punto greenlight dónde vamos a especificar vamos a decir vamos a devolver es un elemento ha sido añadido en el índice encadenamos contador y ahora por supuesto cada vez que pasamos por aquí vamos a decir que encontrado los dos con todos los que sirve para decir que cada vez que pase por aquí quiero añadir un nuevo objeto quiero añadir un elemento al contador y dejaba de dar voy repitiendo de este ciclo hasta que finalicé el bucle en el que estoy aumentando cada vez un elemento al contador bien pues lo que vamos a hacer ahora una vez que tenemos esta clase inscrita y hemos generado un objeto vamos a ir al método principal y vamos a hacer que un método pueda a una matriz pueda contener también con objetos