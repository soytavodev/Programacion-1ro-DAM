voy a replantear esta estructura de control condicional y de hecho la voy a convertir en una multi estructura de control es decir una condición compleja acabo de anular el código anterior es decir lo he puesto con un comentario se ha quedado invalidado y voy a decir si el valor de esto es igual a nada equivale a comillas en este caso digo remove class error remezclas ok en este caso error quiero error añadir clase error remus class ok remove clase warning y mostrar error sí no y aquí es donde viene la complicación o mira si no voy a poner otra estructura de control fijaos que he puesto un if els dentro de otro els porque está anidado si this punto vale es igual a hola en este caso reebok las error remove class warning class ok lo pongo con sangrías para que lo veáis bien sí no remove class ok remove class error haz class warning es una condición compleja que os puede costar de leer al principio que básicamente dice si el valor de esto es nada en este caso error si no es cierto hay que ver si el valor no es cierto que no es nada luego es algo pero no es hola en ese caso warning y si no ok así que fijaos que si no pongo nada es rojo si empiezo a poner algo es amarillo y solo cuando escribo hola se vuelve de color verde tengo por tanto tres estados para un mismo campo