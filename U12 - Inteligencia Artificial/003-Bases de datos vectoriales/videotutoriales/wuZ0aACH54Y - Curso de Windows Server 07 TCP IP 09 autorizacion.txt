una vez que el servicio de hcp ha sido correctamente instalado todavía no ha sido configurado podremos comprobar que nuestro panel de la izquierda aparece la nueva entrada llamada gp pero también vamos a poder comprobar que debemos que estamos viendo el warning que nos dice que tenemos completar la configuración del servidor dhcp así que en este caso vamos a pulsar en la palabra más que encontramos dentro del mensaje donde nos aparece la tarea que tenemos que completar en este caso la tarea nos va a decir vamos a ver por aquí la tarea que tenemos que completar la configuración del chp antes de poderla usar en nuestro equipo así que en primer lugar vamos a hacer clic en completar configuración de hcp tras lo cual veremos que aparece la siguiente ventana en el siguiente paso vamos a poder especificar la autorización para el servidor dhcp usando las credenciales de nuestro active directory por tanto uno de los requisitos para poder instalar de hcp es antes haber instalado una active directory y un controlador de dominio así que vamos en este caso a especificar lo siguiente vamos a especificar que queremos usar el administrador de curso win server para usar sus credenciales como con el objeto de poder especificar que este usuario es el que mandar permiso desde el dominio para poder instalar el servidor así que vamos a ver vamos a darle queremos confirmar si todo va bien vamos a ver a continuación se indica el estado de los pasos de comprado en posteriores en este caso ha creado grupos de seguridad y ha autorizado al servidor dhcp podemos comprobar por tanto que el primer paso de la configuración post instalación del servicio php ya está finalizado simplemente vamos a tener en cuenta qué que pulsemos el servidor próximo sobre todo cerrar no quiere decir que se haya completado la instalación sino que simplemente esta instalación va a proceder a ejecutarse en el background