ahora lo que voy a hacer es justo bajo de los miembros voy a poner los siguientes system junto al punto print line sin parámetros punto y coma era de poder system punto a punto print line y voy a poner lo siguiente del miembro 1 voy a usar el get nombre el nombre de cómo voy a hacer otra vez aquí hay un error system punto out este punto out punto print line y copiado en este caso miembro 1 de apellido era lo más interesante la más interesante que voy a hacer este punto el punto print line y es que el miembro 1 voy a usar el vamos a ver voy a usar este método 10 miembros voy a usar que miembros sea que no tengo ningún error miembros en miembros sin parámetros en este caso me dice que de miembros está accediendo a un método estático así que en principio da un aviso pero no da ningún error vamos a verlo porque si ahora ejecutó este código me devuelve los dos constructores y me dice juan lópez dos en este caso fijémonos que dado que estoy trabajando con el miembro 1 el miembro es debería ser igual a uno se le digo que quiero el miembro 2 el nombre y el apellido miembros me dice que en lugar de juan lópez es alfonso garcía pero sigue siendo 2 por tanto resumiendo lo que hemos hecho es usando un método estático que me devuelva siempre el valor 2 ya pida el miembro 1 el miembro 2 podemos comprobar como get nombre y apellido son variables que dependen de cada calco de cada objeto es decir son únicas para cada instancia el objeto en cambio 10 miembros al ser un método estático es común es igual para todos todas las instancias de los objetos si uno usa una instancia cambia este método cambia el valor cambia automáticamente para todas las instancias de los métodos y si estamos hablando de variables cambiará también para todas las instancias de las variables dentro de los metros