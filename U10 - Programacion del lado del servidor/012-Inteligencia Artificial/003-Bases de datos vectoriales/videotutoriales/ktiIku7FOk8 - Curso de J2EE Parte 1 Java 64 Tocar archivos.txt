hasta ahora dentro de java hemos sido capaces de crear programas sencillos que almacenan valores en variables que manejan en los valores con estructuras de control o que heredan y comparten información entre las clases pero de cara al usuario final es muy común que entendamos que un programa debe escribir y debe leer datos de manera persistente en el disco duro del ordenador que se llama escribir archivos por tanto vamos a ver a continuación una clase de java que se llama file que nos permite leer y nos permite escribir archivos dentro del disco duro para usar esta clase primero debemos importarla y por tanto antes de arrancar la clase pública jose vicente voy a hacer un importe java punto i o iones de inout punto file usando esta clase file importando la voy a ser capaz de usar todos sus métodos lo primero que voy a hacer es crear un objeto y voy a decir es decir va a ser un objeto de trata del tipo de actos file solo llamar x y va a ser igual a una nueva instancia de file entre paréntesis voy a escribir es donde se encuentra ese fael donde se encuentra pues se encuentra aquí se encuentra en 2 puntos barra pruebas barra archivo y punto xd y lo que vamos a hacer en este caso es comprobar que el archivo es el archivo existe o no existe ahora mismo este archivo no existe vamos a verlo vamos a ver este es el disco duro que tengo y voy a crear una carpeta llamada pruebas por alguna parte se habrá creado pruebas aquí está y esta carpeta pruebas no contiene ahora mismo ningún archivo llamado archivo punto txt por tanto lo primero que vamos a hacer es crear una estructura de control que nos diga si el archivo al que estoy apuntando existe o no existe por tanto voy a hacer un primer id y voy a decir si x punto existe nos vemos aquí como predeterminado si x existe entonces haz una cosa y si no existe entonces hace otra cosa y voy a decir lo siguiente si existe de sistema punto out punto print line el archivo existe y si no existe punto out punto print line el archivo no existe así que si ahora ejecutó el programa comprobar como el archivo no existe se verá el archivo no existe pero si voy a la carpeta pruebas y creó el archivo punto txt voy a crear un archivo nuevo de texto archivo que sean exactamente igual que el archivo al que estoy apuntando y ejecute el programa me dirá el archivo existe por tanto tengo ya una estructura de control bueno en este vídeo es cierto no he sido capaz todavía de leer y de escribir nada dentro de un archivo pero ya he sido capaz de detectar algo que está fuera del programa detectar la existencia de un archivo en el disco duro y lanzarlo en pantalla en los siguientes vídeos seré capaz de leer y describir contenido dentro del archivo antes de acabar decir que lo que he hecho es en lugar de escribir con una barra simple escribir con una barra normal en windows si ponemos contra barra voy a poder ahora contra barra el problema es que podría fallar porque las cadenas aquí tenemos un error en las cadenas y tenemos que las contra barras son caracteres de escape y si ejecuta el programa lo que voy a obtener es un fallo por tanto si escribo contra barra lo que le estoy diciendo al programa es que la primera contra barras un carácter de escape y la segunda contra barraza contra barra real por lo que si ejecutó ahora el programa se ejecutará correctamente por eso es por lo que si me pongo una barra normal sólo tengo que poner una barra hasta aquí no hay ningún problema pero si pongo contra barras en windows lo que sería correcto tengo que poner dos para que el programa no crea que la primera contra barra es aunque la contra barra única es un carácter de escape de la cadena y por tanto falla el programa