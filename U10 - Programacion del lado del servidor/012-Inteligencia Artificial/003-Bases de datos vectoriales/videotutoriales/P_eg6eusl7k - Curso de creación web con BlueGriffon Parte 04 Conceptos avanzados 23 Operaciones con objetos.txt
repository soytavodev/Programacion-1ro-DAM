una función bastante interesante de svg y además está implementada correctamente el audífono es el uso de capas si hago doble clic comprobaré como aquí a la derecha en un menú de capas que me permite hacer lo siguiente voy a borrar todos los objetos y en capas voy a crear tengo ya una capa donde dibujo un círculo ahora voy a crear una capa nueva capa 2 donde dibujaré un rectángulo y ahora voy a crear una capa 3 donde crearé una forma predeterminada voy a dibujar por ejemplo un pentágono el problema ahora mismo está en que estos objetos están uno delante del otro por ejemplo ya lo mismo solo puedo trabajar en la capa 3 vengo aquí capa 2 y dibujo en la capa 2 es decir estoy utilizando los objetos por capas pero como podría hacer por ejemplo ahora el círculo que está en la capa 1 estuviera por delante para cambiar el orden por objetos simplemente tengo que mover la capa hacia arriba o hacia abajo por tanto puedo trabajar con capas y lo más lo que es más interesante puedo cambiar el orden de las capas si ahora yo le digo que quiero insertar esta imagen en blue grifón comprobaré atención como si revisó el código el programa además me ha hecho una capa 1 que contiene un círculo una capa 2 que contiene un rectángulo y una capa 3 que contienen en este caso un pad ya que todo aquello que no sea un círculo un rectángulo el pentágono no se considera polígono sino que se considera un paz se considera un recorrido donde debo definir uno a uno los vértices que componen ese paso