vamos a poner en marcha nuestra aplicación y recordad que os recomiendo que siempre nuestra aplicación empiece con el siguiente código 2d function y ahí empiezo a mi aplicación es decir ejecuta esto únicamente solo y únicamente cuando todo el código html haya sido correctamente cargado y ahora digo cuando sobre guardar haga clic voy a ejecutar la función y guarda cuando sobre luego vaya cuando sobre leer haga clic ejecutará la función lee ni que decir tiene que si estoy llamando a la función guarda y estoy llamando a la función lee es porque tengo que crear la función guarda y tengo que crear la función ley así que function guarda y escribo function lee y escribo es decir cuando sobre guardar haga clic ejecutar e guardar y cuando sobre leer haga clic ejecutar e lee