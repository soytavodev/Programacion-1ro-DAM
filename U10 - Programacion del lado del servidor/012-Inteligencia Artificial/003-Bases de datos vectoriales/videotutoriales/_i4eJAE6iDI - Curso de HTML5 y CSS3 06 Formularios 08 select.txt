los campus desplegables o los select en html tiene una manera un poco curiosa de realizar un comentario para indicar que voy a hacer un selecto para definir una sección y para hacer un campo desplegable que en html se llama select tengo que crear una etiqueta select y una etiqueta del cierre select esto es un poco extraño porque es de los muy pocos campos de un formulario que se abren y se cierra todo el resto son inputs pero el select no es un input es un selecto un select tiene opciones así que como tiene opciones dentro del selecto contenidas entre la etiqueta de apertura y la de cierre vamos a poner una etiqueta options option vale igual a por ejemplo dime qué tipo de usuario eres pues un profesor fuera de la etiqueta y cerramos también se cierra la opción en select se cierra tanto el select como la opción por eso el formato es este opción el nombre que aparece y se cierra la opción este es el formato voy a introducir más opciones opción siempre contenido dentro del selecto malware igual alumno y cierro la opción con barra opción acción vale y todas las posibilidades que se nos puedan ocurrir por ejemplo oyente si ahora guardo y recargo observaré voy a poner un verde al final para hacer un retorno de carro con el botón de submit observaré que aparece un desplegable miremos la pantalla donde en ese desplegable solo puedo elegir entre las opciones que vengan premarcadas alumno oyente o profesor en este caso es por tanto este un tipo de respuesta en el que el usuario sólo puede elegir una y además no aparece en el formato radio sino que aparecen en formato lista desplegable aunque eso sí la filosofía del select es realmente parecida al botón de tipo radio en cuanto a que sólo vale una respuesta