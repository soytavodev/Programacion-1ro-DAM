lo interesante de los métodos es que pueden tener parámetros dentro de java por tanto vamos a hacer una nueva clase de java le voy a llamar el método con parámetros finish y la clase se queda grabada aquí aparece aquí y dentro de esta clase voy a escribir algo de código dentro del método con parámetros voy a poner vamos a ver una clase pública llamada método con parámetros y voy a poner un método que es público boyle mensajes simples y va a contener una cadena llamada nombre por tanto dentro de este método voy a poner un código muy sencillo llamado system out punto print line y dentro del pri line voy a poner hola como más el nombre lo que hago por tanto es que aquel nombre que yo pasé aquí lo unirá a él a este código un sistema online me pondré hola más el nombre no tengo sólo un método sino que tengo un método con un parámetro lo que haré a continuación es llamar a este método con este parámetro desde la clase principal y comprobar como poniendo un mensaje simple a un nombre aquí especificar aparece hola y el nombre así que ahora de vuelta a la clase principal y dentro del método principal dentro del método main voy a hacer lo siguiente quiero que el usuario introduzca su nombre dentro de un scanner por tanto lo primero que necesito es importar el script por tanto importó el java punto útil punto scanner vamos a buscarlo por aquí abajo tenemos aquí el scanner punto y coma y dentro del método principal ahora si puedo poner que escáner y pongo entrada es igual a una nueva instancia de scanner existen punto ya que ya tengo una nueva instancia del scanner voy a hacer ahora una nueva instancia del método con parámetros por tanto un método con parámetros y se llamará el objeto método con parámetros objeto será igual a una nueva instancia de método con parámetros voy a poner el momento sin parámetros por tanto ahora lo que hago es system punto a punto print line y digo que introduce tu nombre / como a para finalizar y ahora pongo string nombre es igual a input punto no es entrada next line que estoy haciendo lo que estoy haciendo es poner un escáner y recuperar el valor del escáner en este caso en next line y meterlo dentro de la variable nombre por tanto lo importante de esto es que ahora que he introducido la el método la entrada del scanner dentro de la variable igual a nombre voy a hacer lo siguiente el método con parámetros objetos llamó a mensaje simple ya el localizado el objeto llamo ahora al método y el nombre cuál es el nombre lo dejo así como está ya que es este nombre y ahora no quiere simplemente es ejecutar el código y comprobar lo siguiente ejecutó y aquí abajo me dice introducir un nombre así que jose vicente y ahora al pulsar enter me dice hola jose vicente vamos a repasar lo que he hecho porque es muy interesante y lo que he hecho es simplemente arrancar un scanner con una nueva instancia del scanner y arrancar una nueva instancia de el objeto que he creado aquí arriba de la clase que he creado aquí arriba lo que hago por tanto es pedir tu nombre esa línea en si no vale para nada y guardo el valor del scanner dentro de la variable nombre por tanto lo que hago es llamar al objeto a su método es decir a la clase métodos con parámetros el método mensaje simple y le pasó como parámetro le pasó el nombre si recordamos aquí arriba el método con parámetros punto java lo que hacemos es poner un sistema al tren line hola y el nombre que haya en el parámetro del método esto es una manera muy interesante de trabajar ya que nos permite introducir parámetros en los métodos y nos permite personalizar al final nuestros objetos