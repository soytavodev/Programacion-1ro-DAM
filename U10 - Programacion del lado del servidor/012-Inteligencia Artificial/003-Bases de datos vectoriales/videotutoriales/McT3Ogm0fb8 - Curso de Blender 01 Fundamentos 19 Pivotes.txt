vamos a hablar de los centros de rotación Cuando tenemos varios objetos en pantalla voy a esta caja la voy a duplicar así que duplico sin vinculación la coloco aquí la vuelvo a duplicar la coloco aquí cuando selecciono varios objetos con la tecla mayúsculas selección múltiple si por ejemplo quiero girar observaré como los objetos giran en base a un punto de pivote común esto se controla desde este menú que es el punto de pivote donde por defecto está activado el median Point el punto medio a varios objetos Si en lugar de punto medio escojo puntos individuales comprobaré como en este caso puedo girar a través a partir de los puntos originales de Los pivotes originales de objeto si elijo el elemento activo rotar a partir del último elemento que haya seleccionado el último elemento seleccionado es el elemento activo si cojo este el activo es el último que yo haya seleccionado no tienen por qué ser solo objetos pueden ser objetos y por ejemplo una luz y el último objeto que yo seleccioné es el objeto a partir del cual voy a rotar y a continuación Tenemos también rotar a partir del cursor 3D se pongo el cursor ahí y le digo que quiero rotar a través del cursor lo que voy a hacer es rotar pivotar a través de ese cursor que tengo en la pantalla por tanto estas opciones me permiten controlar no solo la rotación sino también la escala pero son especialmente útiles cuando hablamos de rotación