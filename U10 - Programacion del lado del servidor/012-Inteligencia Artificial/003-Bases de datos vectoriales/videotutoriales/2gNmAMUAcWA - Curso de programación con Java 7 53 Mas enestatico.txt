vamos a darle un par de vueltas más a esto de las variables o los métodos estáticos en este caso dentro de este pico punto java voy a crear un par más de métodos donde uno va a ser public string el nombre sin parámetros va a tener el siguiente código que el nombre voy a borrar este carácter que no hace nada y voy a poner return nombre a continuación público en el apellido sin parámetros que devolverá return apellido y ahora voy a hacer un tercer método público int en este caso antes tenía una variable de estática ahora tengo un método estático que además devuelve un entero que se llama get members ok miembros que tiene el siguiente código de turno miembros tengo por tanto ahora dos variables normales una variable estática un método normal dos métodos más normales y un método estático y ahora vamos a usarlos dentro de la clase josé vicente punto java para ver cuál es la diferencia entre usar estático y no usar estático