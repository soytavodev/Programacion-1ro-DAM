debemos tener en cuenta que el formulario mírame aquí la pantalla tenemos también cuenta que el formulario queráis que no está hecho mediante etiquetas html que al final son la etiqueta vamos a verlas por aquí la etiqueta forma la etiqueta input la actividad text área de la misma manera que hemos utilizado hasta ahora las cosas podemos seguir utilizando por ejemplo sabemos que todo el formulario está metido dentro de una etiqueta llamada form si quiero maquetar el formulario lo que voy a hacer es utilizar esta etiqueta como me voy al css esto sí que es lo único incómodo ya que a partir de ahora vamos a tener que estar saltando del html para ver que queremos utilizar hasta el css para escribir el código correspondiente y le digo todo aquello que sea un form tendrá un week del 50% por ejemplo ocupará media pantalla en margin auto truco que hacía madrid auto centrar los objetos en la pantalla si ahora guardo y recargo comprobar es como el formulario se ha centrado no lo veis correctamente no pasa nada para eso os pongo un morder de un píxel 2 el punteado de color gris y os lo pongo para que veáis exactamente lo que ocupa el formulario además quiero que tenga el formulario un poco de margen verdad con respecto al resto de cosas con respecto a los elementos que contiene por tanto que te iban a decir yo aquí a vis los puntos 15 píxeles veis como ahora el contenedor del formulario tiene algo más del estilo se le puede poner color de fondo se le puede poner una sombra se le puede poner lo que se quiera no pongo sombras para tampoco abusar ya que ya he puesto una en la página principal y otra en el texto tampoco pongo color de fondo para no poner mucho color y en la página web