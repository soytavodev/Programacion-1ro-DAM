los formularios en html están contenidos dentro de la etiqueta form en inglés formulario por eso voy a abrir una etiqueta forma y voy a cerrar una etiqueta de forma todo lo que esté contenido dentro de estas dos etiquetas se considerará un formulario aunque se pueden meter elementos html que no necesariamente sean formularios como por ejemplo texto retornos de carro y cosas así dentro de la etiqueta formal hay varias propiedades que podemos introducir donde las dos más importantes imprescindibles son la acción action igual entre comillas al archivo que procesará el formulario cuando hacemos un formulario lo que hacemos es recolectar información y enviarla a un archivo que lo procesa en esta lección vamos a ver cómo hacer formularios más adelante veremos cómo procesar los formularios por eso en acción llamaremos al archivo que procesa el formulario este archivo del momento no existe hoy todavía no existirá pero lo vamos a llamar un nombre de prueba llamado procesar php además como tiene que ser un archivo que realmente procese información no sólo la muestre vamos a ponerle de momento extensión php que es un archivo de egipto el lenguaje php que sí que nos permite realizar cálculos realizar modificaciones con los datos provistos el nombre evidentemente puede ser el que queráis mi archivo php el que sea porque el nombre en definitiva lo ponemos nosotros y luego si tenemos la acción debemos poner el método puede ser post o puede ser web puede ser post o puede ser gay yo suelo recomendar siempre que sea posible usar el método post ya que el método post es más seguro que el método get con el método get las variables se ven en el navegador las variables están arriba al navegador y esto se traduce en que la gente puede manipular esas variables puede piratear nuestros formularios hay veces sin embargo en los que no se puede elegir y hay que usar get pero siempre que se pueda elegir elegiremos el método post ya que el método post es más seguro porque la información que se envía en el formulario está en las cabeceras y no se puede piratear ni se puede manipular por tanto nuestro formulario ahora mismo tiene una acción que es mi archivo php y tiene un método que pondremos de momento en post si actualizamos si ahora guardamos y actualizamos veremos que en el navegador no pasa nada la etiqueta form no tiene ningún resultado visual simplemente prepara la estructura del documento para que sepa que todo lo que haya entre esas dos etiquetas es un formulario