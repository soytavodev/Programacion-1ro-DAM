los shap Keys suelen ser el método preferido de animación facial en Blender un shape key básicamente trata de la transformación de un objeto en otro objeto cuya única con debe ser que tenga el mismo número de vértices para asegurar que esta conici se cumpla lo que se suele hacer es partir de un objeto original y generar directamente a partir de é todas las variaciones vamos a iniciar un ejemplo sencillo con esta caja que es la caja que tenemos por defecto en Blender dónde vamos a encontrar los shap Keys dentro del menú object Data o datos del objeto Dentro de este menú veremos que tenemos un submenú llamado shap Keys Que de momento está vacío Así que con el objeto que voy a ser la forma original en este caso la caja voy a pinchar el botón más y el programa indica que este objeto ahora mismo es la base la basis si añado una base más sería la i1 podemos renombrar está aquí transformación y pasa llamarse transformación si entramos en modo edición y por ejemplo cogemos un vértice y salimos de modo edición veremos Que aunque hemos movido el vértice de momento no se ha aplicado la transformación por qué porque esta transformación tiene un valor que podemos ver aquí que ahora mismo Está a cero si movemos este valor de 0 a 1 transformaremos la posición original basis en la transformación en este caso la primera que hemos hecho el primer shap key Así que este es el método preferido de animación facial vamos a repetirlo Podemos hacerla aquí 2s trans 2 puede tener el nombre que queramos Y si ahora entramos en modo edición y movemos este objeto este vértice comprobaremos que mientras la transformación mueve el primer vértice trans do mueve el segundo vértice es algo extraño que esto pueda servir para animación facial y por eso antes de meternos con nuestro personaje vamos a hacer un sencillo ejemplo