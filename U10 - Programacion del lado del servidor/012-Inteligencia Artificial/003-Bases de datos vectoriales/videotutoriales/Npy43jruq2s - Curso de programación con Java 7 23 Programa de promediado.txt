vamos a lo que hemos aprendido y con ello vamos a hacer un pequeño programa que en este caso lo que hará es devolverme el promedio de varios números vamos a borrar el código que teníamos que vamos a crear varias variables y crear una variable total que será igual a 0 puede crear una variable que se va a llamar número donde en el momento no le voy a dar ningún valor de una variable llamada promedio que va a ser una variable entera y necesito también una variable de apoyo llamada contador voy a por tanto usar ahora un no while para requerir que el usuario introduzca 10 valores en la pantalla para al final obtener el promedio de esos diez valores así que recordamos que para esto necesitamos antes importar el paquete scanner que nos va a permitir crear y recibir datos del usuario por tanto arriba del todo pongo import java punto útil punto scanner punto y coma veo que tengo una incorrecta cage con un dios y team ports a ponerlo aquí abajo tengo ahora java útil scanner voy a borrar estado primera línea y en mi caso lo que voy a hacer es un while de ante un bucle de control while mire que mientras que el contador sea menor que bien le iré pidiendo cosas al usuario por tanto creo ahora unas llaves y un código a ejecutar dentro del igual y le diré que el número va a ser igual a input punto next y sin parámetros lo que hago es decir que el número será vamos a decirle este input para hacer este impulso y hacerme escáner input la entrada es igual a una nueva instancia de scanner donde cogeré el sistema punto en que recordamos que equivale a la entrada de este claro por tanto el número equivale a la entrada lo que va a hacer es pedir el número introducirlo con la entrada cada vez que introduzca un número lo sumará al total el total es igual al total más el número y por último cada vez que pase por aquí le voy a dar un valor más al contador cuando acabe el wild es decir cuando el contador sea igual o mayor que 10 la cav el wild lo que haré es calcular el promedio será igual al total partido por 10 el partido por 10 y por supuesto si este punto out punto green line y a derecha lo que es sacar el promedio vamos a ejecutar el programa vamos a ver si nos da errores no pasa nada en la pantalla vamos a ver si tengo un error el contador es menor que 10 contador lo tengo aquí y en contador contamos más y ya que estamos tengo que darle no para el error he declarado la variable contador pero no le ha asignado un valor por tanto ahora le asignó un valor y ejecutó el programa ahora parece que sí así que si yo no pongo números y ahora pongo 1 2 3 4 5 6 7 8 9 y 10 me dirá que el número promedio es 5 voy a hacer también el centro de print line poner aquí una encarnación el promedio es promedio así que se hará ejecutó el código otra vez pero que nada errores si ahora podemos seguir escribiendo 1 2 3 4 5 6 7 8 9 y 10 y me dice el promedio es 5 si vuelvo a ejecutar el programa y pongo por ejemplo 35 68 que sea en este caso he puesto una letra por tanto evidentemente ha fallado la ejecución del programa ya que no puede ni sumar letras ni hacer promedio de letras yo sí pongo valores aleatorios teniendo en cuenta siempre que sean números en este caso qué casualidad el promedio ha sido 5 tengo por tanto mediante el uso del bucle while para que ha servido el wish book le while el bucle wild le ha servido para sobre todo aquí pedir hacer que en el programa pida 10 números por tanto siempre que el contador sea menor que 10 cada vez que ha pedido un número ha sumado el valor del número al total y contador lo que ha hecho es asegurar que este bucle se repita un máximo de 10 veces