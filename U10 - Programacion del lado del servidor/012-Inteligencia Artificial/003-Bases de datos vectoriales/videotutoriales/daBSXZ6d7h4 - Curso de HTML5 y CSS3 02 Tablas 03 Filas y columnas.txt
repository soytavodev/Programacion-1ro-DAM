añadir filas dentro de una tabla y añadir columnas también simplemente es una cuestión de repetir las etiquetas tv para columna y t r para fila así que si yo quiero hacer una nueva columna dentro de esta fila si a continuación de esta celda quiero introducir otra lo que quiere es introducir un retorno de carro bajo del cierre de la columna bajo de la etiqueta de cierre de tv e introduzca una buena etiqueta td con su correspondiente cierre evidentemente todo lo que se abre ha de ser cerrado si ahora pongo contenido poner algo de contenido porque si no es posible que la tabla no represente esa celda solo la representa si tiene algo de contenido en concreto y no un punto ya es contenido y pongo contenido 2 si ahora guardo y actualizo observaré como ha aparecido una segunda columna en esta fila tengo miremos el código y miremos el navegador tengo un tr que es una fila por tanto tengo una sola fila en la página web y ahora tengo dos te ves un te ve por aquí otro te ve por aquí tv es columna así que compruebo como en esta fila tengo dos columnas maquetar en tablas es realmente así de sencillo a continuación vamos a introducir otra fila con dos columnas para introducir otra fila tengo que abrir otro perro cerrar el tr ya tengo una fila nueva vemos como aquí abajo se ha abierto una fila nueva vemos cómo fijamos como abajo ese grosor del borde inferior es algo más grande que el del borde superior y dentro de esta nueva fila introducimos columnas pero atención las tablas son un poco cuadriculadas reconozcámoslo por tanto el número de columnas que haya en una fila debe ser igual que el número de columnas que haya en las otras filas que pasa así por ejemplo yo ahora introduzco una columna dv y pongo contenido 3 en una fila tengo dos columnas y en la otra fila tengo una columna que pasará en el html pues que lo que hace es rellenar solo la primera columna y la segunda la deja vacía es un efecto bastante feo de hecho porque es una tabla como que está bloqueada no tiene esa celda por tanto debemos tener cuidado y aunque sea para ponerlas vacías pero debemos poner el mismo número de columnas en todas las filas por tanto ahora voy a hacer otra vez cb y voy a poner un contenido 4 guardo recargo y ahora si tengo contenido 4 si realmente no quería ir ese contenido pues si queréis no dibujes contenido pero tenéis que hacer tv y tv tenéis que abrir y cerrar la columna cuidado porque entonces quedará abierta aunque quedará vacía con que pongáis un solo punto ya se rellenan por tanto las tablas la única complicación que tienen es que deben tener el mismo número de columnas en todas las filas o el mismo número de filas en cada columna lo podéis ver como queráis