el primer punto por el que es recomendable empezar una web es por el index el archivo de base de una web siempre se tiene que llamar index html cuando cualquier navegador llegue a esta carpeta de web cuando esta carpeta de web esté colocada en un servidor automáticamente siempre se abre a que el archivo que tenga el nombre index.html así que creó un documento vacío y lo llamo index.hu ml luego las páginas interiores se pueden llamar como queramos pueden llamar servicios pueden llamar contactos puede llamar productos podrían más sobre nosotros como queráis pero la primera página que suele ser la principal se tiene que llamar index.hu pml le llamamos principal punto este ml por defecto puede no ser reconocida así que index html es un archivo vacío que ahora cargamos en el editor de textos y que cargaremos en el navegar así que el proyecto del curso cargo index.html en el editor de textos y lo cargo también en el navegador de esa manera me aseguró que sea aquí pongo hola aquí ponga bola es decir es el mismo archivo que estoy editando el que aparece tanto en el editor como en el navegador